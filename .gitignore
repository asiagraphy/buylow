# === OS ===
.DS_Store

# === JetBrains IDE ===
.idea/
*.iml

# === Python ===
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# === .NET / C# ===
bin/
obj/
*.user

# === LEAN 런타임/산출물 ===
# 배포 조립 폴더, 백테스트 결과, 로그 등 (실제 경로는 스캐폴딩 시 확정)
runtime/build/
backtests/
*.log

# === 데이터 (대용량·각자 ETL로 생성, repo에 안 올림) ===
data/

# === 로컬 전용 샘플/더미 (공개 repo 미포함) ===
# 합성 KRX 시드 ETL + 더미 삼성 전략. (실 ETL etl/krx.py·sources.py 등은 커밋됨)
etl/seed_krx_sample.py
strategies/SamsungBuyHold.py

# === LEAN Python 런타임 venv (run-backtest.sh가 생성) ===
.leanpy/

# === 로컬 설정·시크릿 (사용자별, 절대 커밋 금지) ===
config.local.yaml
config/*.local.*
.env
.env.local
.env.local.tmp
# 증권사 접근토큰 캐시 (사용자별, 절대 커밋 금지)
.kis_token.json
.toss_token.json

# === 실행 결과 blob (per-run) ===
runs/

# === 로컬 상태 DB (SQLite, 사용자 실행 이력) ===
buylow.db*

# === Docker 런타임 상태 디렉토리 (config.local.yaml·buylow.db·.kis_token.json 모음) ===
state/

.mcp.json
